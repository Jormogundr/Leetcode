#include <string>
#include <vector>
using namespace std;

class Solution {
public:
    string reverseWords(string s) {
        int l = 0; int r = 0;
        int n = s.size();
        int wordCount = 0;


        while (l < n) {
            while (l < n && !isspace(s[l])) {

            }
        }
    }
};